server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /srv/www;

    index index.html;

    server_name _;

    location / {
        try_files $uri $uri/ =404;
    }
}

# Local Variables:
# mode: nginx
# End:
